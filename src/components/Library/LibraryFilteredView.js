// import React, { useContext, useState } from "react"
// const {letter} = useParams()
// import { FindWordContext } from "../FindWord/FindWordProvider";
// import "./Library.css";



// /*  ========PSEUDOCODE ======= 
// 1. User can filter their libraries (that is, lists of saved words) by:

// • starting letter, 
// • length of word, or 
// • chronological order (oldest/newest saved)

// Users can select multiple words to save and delete, using checkboxes, and then submit choices.

// */

// /*  suggested logic for word length
// arr.sort((a, b) => b.length - a.length) 

// suggested logic for retaining alpha-order while sorting for word length
// || // sort by length, if equal then
//          a.localeCompare(b);    // sort by dictionary order

// From MDN:
// students.sort((firstItem, secondItem) => firstItem.grade - secondItem.grade);
         
//          */

// export const sortWordsByLengthInAlpha = () => {
//     const {userWords, getUserWords} = useContext(FindWordContext);
// }


